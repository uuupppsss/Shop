<UserControl x:Class="WpfAdminClient.View.OrderDetailsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfAdminClient.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <StackPanel Margin="10">
                <TextBlock Text="{Binding Order.FullName}" FontWeight="Bold" FontSize="16"/>
                <TextBlock Text="{Binding Order.Cost}" Margin="0,5,0,0"/>
                <TextBlock Text="{Binding Order.Status}" Margin="0,5,0,0"/>
                <TextBlock Text="{Binding Order.Adress}" Margin="0,5,0,0"/>
                <TextBlock Text="{Binding Order.Index}" Margin="0,5,0,0"/>
                <TextBlock Text="{Binding Order.ContactPhone}" Margin="0,5,0,0"/>
                <TextBlock Text="{Binding Order.Trak}" Margin="0,5,0,0"/>
                <TextBlock Text="{Binding Order.CreateDate}" Margin="0,5,0,0" />
            </StackPanel>
            <StackPanel>
                <TextBlock Text="Товары в заказе"/>
                <ListBox ItemsSource="{Binding Items}" >

                </ListBox>
            </StackPanel>
        </StackPanel>
        <Button Content="Редактировать" Grid.Row="1" Command="{Binding EditCommand}"/>
    </Grid>
</UserControl>
